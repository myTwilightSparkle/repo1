<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iotek.hrmgr.mapper.AdminMapper">

    <select id="selectAdminById" parameterType="int" resultType="com.iotek.hrmgr.entity.Admin">
        SELECT * FROM ADMINS WHERE ADMIN_ID=#{adminId}
    </select>

    <select id="selectAllAdmins" resultType="com.iotek.hrmgr.entity.Admin">
        SELECT * FROM ADMINS ORDER BY ADMIN_ID
    </select>

    <insert id="insertAdmin" parameterType="com.iotek.hrmgr.entity.Admin">
        INSERT INTO ADMINS (NAME,PASSWORD,EMAIL) VALUES (#{name},#{password},#{email})
    </insert>

    <update id="updateAdmin" parameterType="com.iotek.hrmgr.entity.Admin">
        UPDATE ADMINS SET (NAME,PASSWORD,EMAIL) VALUES (#{name},#{password},#{email})
    </update>

    <delete id="deleteAdminById" parameterType="int">
        DELETE FROM ADMINS WHERE ADMIN_ID=#{adminId}
    </delete>
</mapper>